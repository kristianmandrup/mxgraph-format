import { FontSizeHandler } from "../../../../";

describe("UpdateCssHandler", () => {
  let instance;
  beforeEach(() => {
    instance = new FontSizeHandler();
  });

  describe("instance", () => {
    describe("properties", () => {});
  });
});
